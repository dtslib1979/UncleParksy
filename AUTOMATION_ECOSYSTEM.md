# 🔄 UncleParksy 자동화 생태계 연동 현황

> **최종 업데이트**: 2025-12-10  
> **리포지토리**: dtslib1979/UncleParksy

---

## 📊 외부 생태계 연동 요약

| 연동 | 방향 | 상태 |
|------|------|------|
| **Tistory → GitHub** | RSS 백업 | ✅ 활성 |
| **GitHub → Obsidian** | HTML/MD 동기화 | ✅ 활성 |

---

## 1️⃣ Tistory RSS 백업

### 설정 정보

| 항목 | 값 |
|------|-----|
| **RSS URL** | `https://dtslib1k.tistory.com/rss` |
| **실행 주기** | 매 6시간 (cron: `0 */6 * * *`) |
| **하루 실행** | 4회 (00:00, 06:00, 12:00, 18:00 UTC) |
| **저장 경로** | `backup/raw/*.html` |
| **파일명 형식** | `YYYY-MM-DD-제목.html` |
| **워크플로우** | `.github/workflows/tistory-backup.yml` |
| **스크립트** | `scripts/tistory_backup.py` |

### 동작 흐름

```
Tistory Blog (dtslib1k.tistory.com)
        │
        ▼ RSS Feed
feedparser 파싱
        │
        ▼ requests
HTML 다운로드
        │
        ▼
backup/raw/YYYY-MM-DD-제목.html
        │
        ▼ git push
GitHub Repository
```

---

## 2️⃣ Obsidian 동기화

### 설정 정보

| 항목 | 값 |
|------|-----|
| **실행 주기** | 매 3시간 (cron: `0 */3 * * *`) + push 트리거 |
| **하루 실행** | 8회 + 수동 push 시 추가 실행 |
| **소스 경로** | `category/**` |
| **저장 경로** | `_obsidian/_imports/` |
| **변환** | HTML → Markdown (pandoc) |
| **워크플로우** | `.github/workflows/obsidian-sync.yml` |

### 출력 구조

```
_obsidian/_imports/
├── category/          # 원본 HTML 복사
│   ├── Philosopher-Parksy/
│   ├── Technician-Parksy/
│   ├── Visualizer-Parksy/
│   └── ...
├── html_raw/          # RAW HTML 백업
└── html_md/           # Markdown 변환본
```

### 로컬 Obsidian 연동

| 항목 | 값 |
|------|-----|
| **로컬 레포** | `C:\Users\dtsli\UncleParksy_Local\UncleParksy` |
| **Obsidian 볼트** | `C:\Users\dtsli\Documents\Obsidian Vault` |
| **GitHub 백업 위치** | `📥 GitHub 백업\UncleParksy\` |

---

## 3️⃣ 전체 데이터 흐름도

```
┌─────────────────────────────────────────────────────────────────┐
│                    UncleParksy 자동화 생태계                      │
└─────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │   Tistory    │
    │    Blog      │
    └──────┬───────┘
           │
           ▼ RSS (6시간마다, 4회/일)
    ┌──────────────┐
    │  backup/raw/ │
    │   *.html     │
    └──────┬───────┘
           │
           ▼ 모바일화 (mobilize.yml)
    ┌──────────────┐
    │   archive/   │
    │   *.html     │
    └──────────────┘


    ┌──────────────┐
    │  category/   │◄─────── 수동 업로드
    │   *.html     │
    └──────┬───────┘
           │
           ▼ 동기화 (3시간마다, 8회/일)
    ┌──────────────────────────────┐
    │     _obsidian/_imports/      │
    │  ├── category/ (HTML 복사)   │
    │  ├── html_raw/ (원본)        │
    │  └── html_md/  (MD 변환)     │
    └──────┬───────────────────────┘
           │
           ▼ git pull (로컬)
    ┌──────────────────────────────┐
    │      Obsidian Vault          │
    │   (로컬 PC)                  │
    └──────────────────────────────┘
```

---

## 4️⃣ 워크플로우 실행 주기 요약

| 워크플로우 | 주기 | 일일 실행 | 트리거 |
|-----------|------|----------|--------|
| `tistory-backup.yml` | 6시간 | 4회 | schedule, manual |
| `obsidian-sync.yml` | 3시간 | 8회 | schedule, push, manual |
| `obsidian-backup.yml` | push | - | push to category/** |
| `category-index.yml` | push | - | push to category/**/*.html |
| `pages-maintenance.yml` | push | - | push to category/**, index.html |
| `build-textstory.yml` | 3시간 | 8회 | schedule, push, manual |
| `mobilize.yml` | 6시간 | 4회 | schedule, push, manual |

---

## 5️⃣ 수동 실행 방법

### GitHub Actions 수동 트리거

```bash
# Tistory 백업 수동 실행
gh workflow run tistory-backup.yml

# Obsidian 동기화 수동 실행
gh workflow run obsidian-sync.yml

# 전체 카테고리 인덱스 재생성
gh workflow run category-index.yml
```

### 로컬 동기화

```bash
cd C:\Users\dtsli\UncleParksy_Local\UncleParksy
git pull origin main
```

---

## 📌 핵심 정리

1. **외부 연동은 2개**: Tistory RSS 백업 + Obsidian 동기화
2. **Tistory**: 6시간마다 자동 백업 (하루 4회)
3. **Obsidian**: 3시간마다 + push마다 동기화 (하루 8회+)
4. **로컬 Obsidian**: `📥 GitHub 백업\UncleParksy\` 폴더에서 확인

---

*🤖 자동 생성 by EduArt Engineer CI*
