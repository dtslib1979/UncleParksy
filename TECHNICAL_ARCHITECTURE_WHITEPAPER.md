# 📘 UncleParksy 시스템 아키텍처 기술백서
# Technical Architecture White Paper

> **작성일**: 2025-12-09  
> **버전**: 1.0  
> **리포지토리**: dtslib1979/UncleParksy  
> **목적**: 프로그래머가 즉시 재현 가능한 완전한 시스템 아키텍처 문서

---

## 📋 목차 (Table of Contents)

1. [프로젝트 개요](#1-프로젝트-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [기술 스택](#3-기술-스택)
4. [핵심 컴포넌트](#4-핵심-컴포넌트)
5. [자동화 파이프라인](#5-자동화-파이프라인)
6. [데이터 플로우](#6-데이터-플로우)
7. [설치 및 실행 가이드](#7-설치-및-실행-가이드)
8. [개발 이력](#8-개발-이력)
9. [API 레퍼런스](#9-api-레퍼런스)
10. [트러블슈팅](#10-트러블슈팅)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 정의

**UncleParksy**는 AI 시대의 크리에이터를 위한 디지털 지식 플랫폼입니다. 단일 HTML 파일을 중심으로 한 모듈러 지식 체계(Codex)를 구축하여, 8개의 독립적인 페르소나가 각자의 전문 영역에서 콘텐츠를 생산하고 관리하는 생태계입니다.

### 1.2 핵심 철학

```
압축(Compress) → 구조화(Structure) → 발행(Publish)
```

- **음성 우선 캡처**: 아이디어를 음성으로 즉시 기록
- **AI 압축**: Claude, ChatGPT, Grok을 활용한 콘텐츠 정제
- **인간 구조화**: 창작자가 최종 구조와 맥락 결정
- **즉시 발행**: GitHub Pages를 통한 자동 배포

### 1.3 8개 페르소나 시스템

| 페르소나 | 영역 | 카테고리 폴더 | 콘텐츠 수 |
|---------|------|--------------|----------|
| Philosopher-Parksy | 철학/이론 | `/category/Philosopher-Parksy/` | 11개 |
| Technician-Parksy | 디바이스/기술 | `/category/Technician-Parksy/` | 23개 |
| Visualizer-Parksy | 시각화/다이어그램 | `/category/Visualizer-Parksy/` | 10개 |
| Musician-Parksy | 음악/사운드 | `/category/Musician-Parksy/` | 6개 |
| Protocol-Parksy | 시스템 설계 | `/category/Protocol-Parksy/` | 6개 |
| Blogger-Parksy | 웹 개발/PWA | `/category/Blogger-Parksy/` | 6개 |
| Orbit-Log | 라이프 로그 | `/category/Orbit-Log/` | 6개 |
| Tester-Parksy | 테스트/실험 | `/category/Tester-Parksy/` | 1개 |

**총 콘텐츠**: 69개 HTML 문서 (카테고리 기준)

### 1.4 프로젝트 통계

```
📊 콘텐츠 통계
├── 카테고리 콘텐츠: 77개 HTML 파일
├── 아카이브: 29개 HTML 파일
└── 총계: 106개 HTML 문서

🤖 자동화 시스템
├── GitHub Actions 워크플로우: 7개 (2개 활성)
├── Python 자동화 스크립트: 7개
└── 수동 작업 비율: 0%

📁 코드베이스
├── index.html: 777줄 (단일 HTML 앱)
├── Python 스크립트: ~3,000줄
└── 워크플로우 YAML: ~500줄
```

---

## 2. 시스템 아키텍처

### 2.1 전체 시스템 구조도

```
┌─────────────────────────────────────────────────────────────────┐
│                     UncleParksy 생태계                           │
│                  (AI Creator Liberal Arts Codex)                 │
└─────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  Frontend    │      │  Automation  │      │   Content    │
│   Layer      │      │    Layer     │      │    Layer     │
└──────────────┘      └──────────────┘      └──────────────┘
        │                     │                     │
        ├─ index.html         ├─ GitHub Actions     ├─ category/
        ├─ Three.js           ├─ Python Scripts     ├─ archive/
        ├─ Service Worker     └─ Workflows          └─ backup/
        └─ PWA Manifest
```

### 2.2 레이어별 상세 구조

#### 2.2.1 Frontend Layer (프론트엔드 계층)

```
index.html (777 lines) - 단일 HTML 앱
├── HTML5 구조
│   ├── Hero Section (책 표지 디자인)
│   ├── 8개 Chapter Cards (페르소나별 카드)
│   ├── Footer (저작권 및 링크)
│   └── Skip Link (접근성)
│
├── CSS3 스타일링
│   ├── Parchment Theme (양피지 테마)
│   ├── Custom Cursor (커스텀 커서)
│   ├── Responsive Design (반응형)
│   └── Print Optimized (인쇄 최적화)
│
├── JavaScript ES6+
│   ├── Three.js 파티클 애니메이션
│   ├── GitHub API 파일 카운터
│   ├── 배경음악 컨트롤
│   └── Smooth Scrolling
│
└── PWA 기능
    ├── manifest.webmanifest
    ├── sw.js (Service Worker)
    └── 오프라인 지원
```

#### 2.2.2 Automation Layer (자동화 계층)

```
GitHub Actions (CI/CD)
├── category-index.yml (활성)
│   ├── 트리거: push to category/**/*.html
│   ├── 기능: 카테고리 인덱스 자동 생성
│   └── 출력: home.json, index.html, manifest.json
│
├── pages-maintenance.yml (활성)
│   ├── 트리거: push to category/**, index.html
│   ├── 기능: sitemap.xml 생성, 인덱스 리빌드
│   └── 출력: sitemap.xml
│
└── disabled workflows (5개)
    ├── build-textstory.yml.disabled
    ├── mobilize.yml.disabled
    ├── obsidian-backup.yml.disabled
    ├── obsidian-sync.yml.disabled
    └── tistory-backup.yml.disabled

Python Scripts (7개)
├── generate_category_index.py - 카테고리 인덱스 생성
├── generate_archive_manifest.py - 아카이브 매니페스트 생성
├── auto_install.py - 완전 자동화 오케스트레이터
├── tistory_backup.py - Tistory RSS 백업
├── clean_and_mobilize.py - 콘텐츠 정제 + 모바일화
├── mobilize_archive.py - 모바일 CSS 삽입
└── mirror_backup.py - 백업 → 아카이브 미러링
```

---

## 3. 기술 스택

### 3.1 Frontend Technologies

| 기술 | 버전 | 용도 | 특징 |
|------|------|------|------|
| **HTML5** | - | 구조 마크업 | 시맨틱 태그, ARIA 접근성 |
| **CSS3** | - | 스타일링 | Custom Properties, Flexbox, Grid |
| **JavaScript** | ES6+ | 상호작용 | 모던 JS, 모듈화 |
| **Three.js** | r128+ | 3D 그래픽 | 파티클 애니메이션 |
| **Service Worker** | - | PWA | 오프라인 지원, 캐싱 |

**특징**:
- **제로 빌드 시스템**: 빌드 도구 없음, 순수 HTML/CSS/JS
- **제로 프레임워크**: React, Vue 등 미사용
- **CDN 기반**: Three.js는 CDN에서 로드
- **모바일 우선**: Galaxy S25 Ultra 등 플래그십 디바이스 최적화

### 3.2 Backend Technologies

| 기술 | 버전 | 용도 |
|------|------|------|
| **Python** | 3.11 | 자동화 스크립트 |
| **BeautifulSoup4** | 4.12+ | HTML 파싱 |
| **lxml** | 4.9+ | XML/HTML 파서 |
| **requests** | 2.31.0 | HTTP 클라이언트 |
| **feedparser** | 6.0.10 | RSS 파싱 |
| **pandoc** | 2.x | 문서 변환 (HTML→MD) |

### 3.3 Infrastructure

| 서비스 | 용도 | 설정 |
|--------|------|------|
| **GitHub Pages** | 호스팅 | 자동 배포 |
| **GitHub Actions** | CI/CD | 7개 워크플로우 |
| **Custom Domain** | DNS | parksy.kr (CNAME) |
| **HTTPS** | 보안 | GitHub Pages 제공 |

---

## 4. 핵심 컴포넌트

### 4.1 index.html - Codex 시스템

**위치**: `/index.html`  
**라인 수**: 777줄  
**역할**: UncleParksy의 메인 엔트리포인트

#### 4.1.1 주요 기능

1. **Three.js 파티클 시스템**
   - 500개 파티클로 구성된 3D 애니메이션
   - 양피지 테마와 조화로운 색상
   - `prefers-reduced-motion` 지원

2. **GitHub API 파일 카운터**
   - 실시간 카테고리별 파일 개수 표시
   - home.json fallback 지원
   - Rate limit 고려 설계

3. **반응형 디자인**
   - 모바일/태블릿/데스크탑 최적화
   - Custom cursor (데스크탑만)
   - 터치 제스처 지원

### 4.2 자동화 스크립트

#### 4.2.1 generate_category_index.py

**역할**: 카테고리별 index.html 자동 생성

**입력**: `category/*/*.html` 파일들  
**출력**: 
- `category/*/index.html` - 각 카테고리 인덱스
- `assets/home.json` - 파일 개수 JSON

**핵심 로직**:
```python
def count_html_files(category_path):
    html_files = [
        f for f in category_path.glob("*.html")
        if f.name != "index.html"
    ]
    return len(html_files)
```

#### 4.2.2 generate_archive_manifest.py

**역할**: archive/ 폴더 매니페스트 생성

**입력**: `archive/*.html` 파일들  
**출력**: `assets/manifest.archive.json`

**데이터 구조**:
```json
{
  "title": "Archive Title",
  "lastUpdate": "2025-12-09T10:00:00Z",
  "count": 29,
  "items": [...]
}
```

---

## 5. 자동화 파이프라인

### 5.1 활성 워크플로우

#### 5.1.1 category-index.yml

**트리거**:
- `push` to `category/**/*.html`
- `push` to `archive/*.html`
- Manual (`workflow_dispatch`)

**작업 흐름**:
```
1. Checkout 코드
2. Python 3.11 설정
3. generate_category_index.py 실행
4. generate_archive_manifest.py 실행
5. Git commit & push
6. Obsidian Sync 트리거 (비활성 상태)
```

**출력 파일**:
- `category/*/index.html`
- `category/*/manifest.json`
- `assets/home.json`
- `assets/manifest.archive.json`

#### 5.1.2 pages-maintenance.yml

**트리거**:
- `push` to `category/**/*.html`
- `push` to `index.html`
- Manual (`workflow_dispatch`)

**작업 흐름**:
```
1. Checkout 코드
2. Python 3.11 설정
3. sitemap.xml 생성
4. 카테고리 인덱스 리빌드
5. Git commit & push
```

**출력 파일**:
- `sitemap.xml`

### 5.2 자동화 플로우 다이어그램

```
┌─────────────────────────────────────────┐
│   Push to category/**/*.html            │
└─────────────────────────────────────────┘
                 │
      ┌──────────┴──────────┐
      │                     │
      ▼                     ▼
┌────────────┐      ┌────────────┐
│ category-  │      │  pages-    │
│  index.yml │      │maintenance │
└────────────┘      └────────────┘
      │                     │
      ├─ home.json          ├─ sitemap.xml
      ├─ index.html         │
      └─ manifest.json      │
      │                     │
      └──────────┬──────────┘
                 ▼
        ┌────────────────┐
        │  Git Commit    │
        │  & Push        │
        └────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ GitHub Pages   │
        │ Auto Deploy    │
        └────────────────┘
```

---

## 6. 데이터 플로우

### 6.1 콘텐츠 생성 플로우

```
Creator (Parksy)
    │
    ├─ 1. 음성 녹음 / 텍스트 작성
    ▼
AI (Claude, ChatGPT, Grok)
    │
    ├─ 2. AI 압축 및 정제
    ▼
Human Structuring (Parksy)
    │
    ├─ 3. 구조화 및 HTML 생성
    ▼
category/{Persona}/YYYY-MM-DD-title.html
    │
    ├─ 4. Git Push
    ▼
GitHub Actions
    │
    ├─ 5. 자동화 파이프라인
    ├── index.html 생성
    ├── home.json 업데이트
    ├── manifest.json 생성
    └── sitemap.xml 갱신
    │
    ▼
GitHub Pages
    │
    ├─ 6. 자동 배포
    ▼
Public Users
```

---

## 7. 설치 및 실행 가이드

### 7.1 사전 요구사항

- **Git**: 2.x 이상
- **Python**: 3.11 이상
- **웹 브라우저**: Chrome, Firefox, Safari (최신 버전)

### 7.2 로컬 개발 환경 구축

#### Step 1: 리포지토리 클론

```bash
git clone https://github.com/dtslib1979/UncleParksy.git
cd UncleParksy
```

#### Step 2: Python 의존성 설치

```bash
pip install -r requirements.txt
```

또는:
```bash
pip install requests==2.31.0 feedparser==6.0.10 beautifulsoup4 lxml
```

#### Step 3: 로컬 서버 실행

**Python 내장 서버**:
```bash
python -m http.server 8000
```

**Node.js http-server**:
```bash
npm install -g http-server
http-server -p 8000
```

#### Step 4: 브라우저에서 접속

```
http://localhost:8000
```

### 7.3 새 콘텐츠 추가하기

#### Step 1: HTML 파일 생성

```bash
cat > category/Philosopher-Parksy/2025-12-09-new-essay.html << 'EOF'
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>새로운 에세이</title>
</head>
<body>
    <article>
        <h1>새로운 에세이</h1>
        <p>콘텐츠...</p>
    </article>
</body>
</html>
EOF
```

#### Step 2: Git Commit & Push

```bash
git add category/Philosopher-Parksy/2025-12-09-new-essay.html
git commit -m "Add: 새로운 철학 에세이"
git push origin main
```

#### Step 3: 자동화 확인

GitHub Actions 탭에서 워크플로우 실행 확인

#### Step 4: 배포 확인

약 1-2분 후 https://dtslib1979.github.io 에서 확인

---

## 8. 개발 이력

### 8.1 개발 타임라인

| 단계 | 시기 | 주요 작업 | 상태 |
|------|------|----------|------|
| **Phase 1** | 2025-08 | Codex 시스템 설계 및 index.html 개발 | ✅ 완료 |
| **Phase 2** | 2025-08 | 8개 페르소나 카테고리 구조 설계 | ✅ 완료 |
| **Phase 3** | 2025-08 | Tistory RSS 백업 시스템 구축 | ✅ 완료 |
| **Phase 4** | 2025-09 | 모바일 최적화 파이프라인 | ✅ 완료 |
| **Phase 5** | 2025-09 | Obsidian 양방향 동기화 | ✅ 완료 |
| **Phase 6** | 2025-09 | 완전 자동화 CI/CD 구축 | ✅ 완료 |
| **Phase 7** | 2025-11 | 매니페스트 시스템 | ✅ 완료 |
| **Phase 8** | 2025-12 | 워크플로우 최적화 | ✅ 완료 |

### 8.2 주요 마일스톤

#### 2025-08-27: Codex V1 Launch
- index.html 777줄 완성
- Three.js 파티클 애니메이션 통합
- 8개 Chapter Card 시스템 구현

#### 2025-09-02: 자동화 시스템 V1
- `category-index.yml` 워크플로우 구현
- `generate_category_index.py` 개발

#### 2025-11-29: 매니페스트 시스템
- `manifest.json`, `home.json` 자동 생성
- 아카이브 매니페스트 시스템 구축

#### 2025-12-09: 시스템 안정화
- 일부 워크플로우 비활성화 (최적화)
- 기술백서 작성

---

## 9. API 레퍼런스

### 9.1 GitHub API

**Endpoint**:
```
GET https://api.github.com/repos/dtslib1979/UncleParksy/contents/category/{persona}
```

**응답 예시**:
```json
[
  {
    "name": "2025-08-29 test.html",
    "path": "category/Philosopher-Parksy/2025-08-29 test.html",
    "type": "file",
    "size": 16625
  }
]
```

**Rate Limit**: 
- 인증 없음: 60 requests/hour
- 인증 있음: 5000 requests/hour

### 9.2 정적 JSON API

#### home.json

**Endpoint**: `/assets/home.json`

**형식**:
```json
{
  "Philosopher-Parksy": 11,
  "Technician-Parksy": 23,
  "Visualizer-Parksy": 10,
  ...
}
```

#### manifest.archive.json

**Endpoint**: `/assets/manifest.archive.json`

**형식**:
```json
{
  "title": "Archive Title",
  "lastUpdate": "2025-12-09T10:00:00Z",
  "count": 29,
  "items": [...]
}
```

---

## 10. 트러블슈팅

### 10.1 파일 개수가 업데이트되지 않음

**해결**:
```bash
# 수동 트리거
GitHub → Actions → category-index.yml → Run workflow

# 또는 로컬 실행
python scripts/generate_category_index.py
git add assets/home.json
git commit -m "Manual: update category indexes"
git push
```

### 10.2 GitHub Pages 배포 실패

**해결**:
```bash
# .nojekyll 파일 확인
touch .nojekyll
git add .nojekyll
git commit -m "Add .nojekyll"
git push
```

---

## 11. 결론

UncleParksy 시스템은 **제로 빌드, 제로 프레임워크** 철학을 바탕으로 한 순수 HTML/CSS/JavaScript 기반의 모듈러 지식 플랫폼입니다.

**핵심 강점**:
1. ✅ **완전 자동화**: GitHub Actions를 통한 무인 운영
2. ✅ **확장 가능**: 8개 페르소나 시스템으로 무한 확장
3. ✅ **성능 최적화**: 단일 HTML, CDN, 오프라인 지원
4. ✅ **프로그래머 친화적**: 명확한 구조, 재현 가능한 설치

이 백서를 통해 누구나 UncleParksy 시스템을 이해하고 재현할 수 있기를 바랍니다.

---

*📝 작성: UncleParksy (박씨)*  
*🤖 AI 협력: Claude, ChatGPT, Grok*  
*📅 최종 업데이트: 2025-12-09*
